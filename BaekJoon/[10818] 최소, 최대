# my solution

import sys

N = int(input())
num_list = list(map(int, sys.stdin.readline()))

print(min(num_list), max(num_list))



# another solution

import sys

N = int(input())
num_list = list(map(int, sys.stdin.readline()))

max = num_list[0]  # max, min reset
min = num_list[0]

for i in num_list[1:] :
  if i > max :
    max = i
  
  else i < min :
    min = i
    
print(min, max)
 
 
 개인적인 생각) 두번쨰 방법이 안좋은 이유는 시간복잡도가 O(n) 이기 때문에
